{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && (prisma migrate deploy 2>/dev/null || echo 'Migration skipped - DB may be paused') && next build",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/webhooks/evolution/route.ts": {
      "maxDuration": 60
    },
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "crons": [{
    "path": "/api/cron/cleanup",
    "schedule": "0 3 * * *"
  }]
}
