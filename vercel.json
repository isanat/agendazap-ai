{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cp prisma/schema.postgresql.prisma prisma/schema.prisma && prisma generate && (prisma migrate deploy || echo 'MIGRATE_DEPLOY_FAILED') && next build",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/webhooks/evolution/route.ts": {
      "maxDuration": 60
    },
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "crons": [{
    "path": "/api/cron/cleanup",
    "schedule": "0 3 * * *"
  }]
}
